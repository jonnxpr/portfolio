# Speckit Context Files

This directory stores repo-local context summaries generated by
`.specify/scripts/powershell/update-agent-context.ps1`.

- Files in this directory are safe-parity artifacts.
- They summarize the active feature plan for a specific tool or agent.
- They must never be used to rewrite home-dir configs or unrelated governance
  files.
